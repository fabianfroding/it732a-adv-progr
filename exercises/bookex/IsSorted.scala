Object IsSorted {
    def isSorted[A](as: Array[A], gt: (A,A) => Boolean): Boolean = {
        if (as[0] > as[1]) {
            false
        }
        else {

        }
    }

    def formatIsSorted(b: Boolean) = {
        val msg = 
    }

    def main(args: Array[String]): Unit = {
        println()
    }
}
